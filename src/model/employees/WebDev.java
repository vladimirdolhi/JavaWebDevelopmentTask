package model.employees;

import model.employees.Dev;

public class WebDev extends Dev {
    public WebDev(String name, String sname, String position, int salary) {
        super(name, sname, position, salary);
    }
}
